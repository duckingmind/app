# 参考页面与素材来源

参考页面：[DesignKit 服装试穿](https://www.designkit.cn/product-kit/try-on)。页面访问和素材下载日期：2026-09-08。

`download/manifest.json` 保留从页面公开图片资源下载记录的 44 项来源信息，包括 URL、媒体类型、下载标识和文件名。原始响应文件不进入公开 Git 仓库；若要在本地复现研究下载，请先确认对应资源的许可。资源来源域名为 `xiuxiu-pro-new.meitudata.com`。

清单的 `path` 是下载工具当时使用的临时路径。仓库中的实际文件位于 `download/` 下，文件名为该路径的末段。CDN URL 包含缩放与格式转换参数，因此这里的“原始”指保留下载响应，并不表示原作者的全尺寸源文件；部分文件扩展名与实际媒体格式可能不同。

## 应用内素材

应用使用上述图片的 44 份优化 WebP，存放于 `../src/assets/`，编译时作为内联资源加载。模特参考可直接参与图片处理，避免生产 iframe 的不透明来源使 Canvas 因跨域素材而不可读。

| 优化文件 | 对应 `assets` 数组下标 | 用途 |
| --- | --- | --- |
| `sample-00.webp` 至 `sample-18.webp` | 0 至 18 | 服饰参考与试穿参考作品 |
| `model-01.webp` 至 `model-25.webp` | 19 至 43 | 模特参考库 |

页面中的三组参考素材组合为：

| 组合 | 服饰文件编号 | 参考作品文件编号 |
| --- | --- | --- |
| 白色 Polo | 00 | 01、02、03 |
| 夏日穿搭 | 10 | 12、13、15 |
| 多件搭配 | 04、06、08 | 07、09、11 |

## 参考与实现的区别

来源记录用于研究可见的工作流程和构建参考素材展示。模特、服饰和参考作品均来自原站公开页面，相关权利属于各自权利人；公开可访问并不等于获得再分发或商业使用授权。仓库仅保留已优化并随应用运行的演示素材；发布到其他渠道前仍应替换为自有或已获授权素材。

衣境设计室是当前仓库框架下的独立应用，真实创作调用 Proxy 平台公开模型 API。没有获取 DesignKit 的专有模型、服务端代码、训练数据或私有生成接口。参考作品也不是本应用生成质量的验证结果。
